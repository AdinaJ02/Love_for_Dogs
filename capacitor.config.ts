import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Love For Dogs',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
